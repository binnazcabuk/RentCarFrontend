import { Byte } from "@angular/compiler/src/util";

export interface Car {
    carId: number;
    brandName: string;
    colorName: string;
    dailyPrice: number;
    description: string;
    modelYear: string;
    imagePath:string;
  }
  